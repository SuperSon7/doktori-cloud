# Nginx stub_status for Prometheus exporter
server {
    listen 8888;
    server_name localhost;
    access_log off;

    location /nginx_status {
        stub_status;
        allow 127.0.0.1;
        allow 10.0.0.0/8;
        deny all;
    }

    location / {
        return 404;
    }
}