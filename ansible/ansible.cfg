[defaults]
host_key_checking = False
stdout_callback = yaml
timeout = 60
roles_path = roles
remote_tmp = /tmp/.ansible-${USER}/tmp

[privilege_escalation]
become = False

[ssh_connection]
pipelining = True
