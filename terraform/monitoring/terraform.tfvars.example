project_name = "doktori"
aws_region   = "ap-northeast-2"

# ── 인스턴스 ──
architecture   = "arm64"        # arm64 (t4g) or x86_64 (t3)
instance_type  = "t4g.medium"   # 4GB RAM — Prometheus + Loki + Grafana
key_name       = "doktori-monitoring"
root_volume_size = 30           # GB — 산정 근거: variables.tf 참조

# ── 네트워크 보안 ──
# TODO: 새 계정 인프라 프로비저닝 후 실제 IP로 교체
allowed_admin_cidrs = [
  # "xxx.xxx.xxx.xxx/32",  # 관리자 1
  # "xxx.xxx.xxx.xxx/32",  # 관리자 2
]

target_server_cidrs = [
  # "xxx.xxx.xxx.xxx/32",  # prod 서버 EIP
  # "xxx.xxx.xxx.xxx/32",  # dev 서버 EIP
]
